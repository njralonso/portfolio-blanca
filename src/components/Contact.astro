<div>
	<p class="mb-12 text-center text-5xl mx-auto text-balance text-white">
		Para consultas de disponibilidad, presupuestos a medida o cualquier otra
		inquietud, te invitamos a ponerte en contacto. Con gusto te asistiremos en
		la planificación de tu servicio de belleza profesional, garantizando un
		resultado impecable y a la altura de tus expectativas.
	</p>
	<form class="space-y-6 mx-auto text-white lg:max-w-4xl">
		<div class="grid grid-cols-1 gap-y-6 gap-x-8 sm:grid-cols-2">
			<div>
				<label for="name" class="block text-6xl lg:text-2xl font-medium">
					Nombre
				</label>
				<div class="mt-4">
					<input
						type="text"
						name="name"
						id="name"
						autocomplete="given-name"
						class="block w-full text-4xl lg:text-xl h-24 lg:h-auto rounded-md border border-gray-600 bg-transparent px-4 py-2 focus:border-white focus:outline-none focus:ring-0 placeholder:text-3xl lg:placeholder:text-lg"
						placeholder="Tú nombre"
					/>
				</div>
			</div>

			<div>
				<label for="surname" class="block text-6xl lg:text-2xl font-medium">
					Apellido
				</label>
				<div class="mt-4">
					<input
						type="text"
						name="surname"
						id="surname"
						autocomplete="family-name"
						class="block w-full text-4xl lg:text-xl h-24 lg:h-auto rounded-md border border-gray-600 bg-transparent px-4 py-2 focus:border-white focus:outline-none focus:ring-0 placeholder:text-3xl lg:placeholder:text-lg"
						placeholder="Tú apellido"
					/>
				</div>
			</div>
		</div>

		<div>
			<label for="email" class="block text-6xl lg:text-2xl font-medium">
				Email
			</label>
			<div class="mt-4">
				<input
					type="email"
					name="email"
					id="email"
					autocomplete="email"
					class="block w-full text-4xl lg:text-xl h-24 lg:h-auto rounded-md border border-gray-600 bg-transparent px-4 py-2 focus:border-white focus:outline-none focus:ring-0 placeholder:text-3xl lg:placeholder:text-lg"
					placeholder="Tú email"
				/>
			</div>
		</div>

		<div>
			<label for="phone" class="block text-6xl lg:text-2xl font-medium">
				Teléfono
			</label>
			<div class="mt-4">
				<input
					type="tel"
					name="phone"
					id="phone"
					autocomplete="tel"
					class="block w-full text-4xl lg:text-xl h-24 lg:h-auto rounded-md border border-gray-600 bg-transparent px-4 py-2 focus:border-white focus:outline-none focus:ring-0 placeholder:text-3xl lg:placeholder:text-lg"
					placeholder="Tú teléfono"
				/>
			</div>
		</div>

		<div>
			<label for="message" class="block text-6xl lg:text-2xl font-medium">
				Descripción
			</label>
			<div class="mt-4">
				<textarea
					id="message"
					name="message"
					rows="4"
					class="block w-full text-4xl lg:text-xl h-24 lg:h-auto rounded-md border border-gray-600 bg-transparent px-4 py-2 focus:border-white focus:outline-none focus:ring-0 placeholder:text-3xl lg:placeholder:text-lg"
					placeholder="Escribe aquí los detalles de tu consulta..."></textarea>
			</div>
		</div>

		<div class="pt-6">
			<button
				class="relative w-full text-8xl lg:text-4xl overflow-hidden rounded-md border border-transparent px-6 py-3 font-medium shadow-sm transition-colors duration-300 before:absolute before:inset-0 before:-translate-x-full before:transform before:bg-[linear-gradient(45deg,#a87f3e,#e9d7a2,#a87f3e)] before:transition-transform before:duration-200 before:ease-out hover:before:translate-x-0 bg-[linear-gradient(45deg,#a87f3e,#e9d7a2,#a87f3e)]"
			>
				<span class="relative z-10 text-black">Enviar</span>
			</button>
		</div>
	</form>
</div>

<script>
	import { actions } from "astro:actions";

	const form = document.querySelector("form");
	form?.addEventListener("submit", async (event) => {
		event.preventDefault();
		const formData = new FormData(form);
		const { error: contactError } = await actions.sendContact(formData);
		const { error: emailError } = await actions.sendEmail(formData);
		if (contactError || emailError) {
			alert(
				"Hubo un error al enviar el formulario. Por favor, inténtalo de nuevo.",
			);
			return;
		}
	});
</script>
